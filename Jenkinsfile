node {
    stage('Clean workspace') {
        cleanWs()
        echo "done"
    }
    stage('Checkout Repository') {
        checkout scm
        echo "done"
    }
    stage('Clean workspace') {
        cleanWs()
        echo "done"
    }
}